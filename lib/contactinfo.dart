import 'package:flutter/material.dart';

class Contactinfo extends StatelessWidget {
  const Contactinfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Container(height: 400, width: 400, color: Colors.amber)],
      ),
    );
  }
}
